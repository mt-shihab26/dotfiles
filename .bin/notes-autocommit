#!/bin/bash

set -e

REPO_DIR="$HOME/Notes"

cd "$REPO_DIR"

git add -A

if git diff --cached --quiet; then
    exit 0
fi

git commit -m "Auto-commit: $(date '+%Y-%m-%d %H:%M:%S')"
git push origin HEAD
