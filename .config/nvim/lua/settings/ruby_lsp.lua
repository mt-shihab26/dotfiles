return {
    enabled = true,
    init_options = {
        enabledFeatures = {
            diagnostics = false,
            codeActions = false,
            formatting = false,
        },
        formatter = "none",
    },
}
